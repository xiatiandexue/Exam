server: 
    port: 8092
    servlet:
      context-path: /Exam
spring:
    datasource:
        username: root
        password: root
        url: jdbc:mysql://localhost:3307/examination?useUnicode=true&characterEncoding=utf8&serverTimezone=UTC
        driver-class-name: com.mysql.cj.jdbc.Driver
        
mybatis:
    mapper-locations: classpath:mapper/*.xml
    config-locations: classpath:mybatis.xml
    type-aliases-package: com.oxy.model

#  mapper-locations=classpath: mapper/*.xml 
